{% extends 'base.html.twig' %}

{% block title %}Modifier User{% endblock %}

{% block body %}

<div style="display: flex;align-items: center;justify-content: center;margin-top :150px">

          <div class="card">
            <div class="card-body">
              <h5 class="card-title">Modifier User :</h5>

              <!-- General Form Elements -->

                

              {{ form_start(f) }}
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-7 col-form-label">firstNameUser</label>
                  <div class="col-sm-10">
              {{ form_widget(f.firstNameUser,{'attr': {'class':'form-control'}}) }}
                  </div>
                </div>

                 {{ form_start(f) }}
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-7 col-form-label">LastName</label>
                  <div class="col-sm-10">
              {{ form_widget(f.LastName,{'attr': {'class':'form-control'}}) }}
                  </div>
                </div>

                {{ form_start(f) }}  
                <div class="row mb-3">
                
                  <label for="inputText" class="col-sm-7 col-form-label">TypeUser</label>
                  <div class="col-sm-10">
              {{ form_widget(f.Role,{'attr': {'class':'form-control'}}) }}
                  </div>
                </div>


                

                   {{ form_start(f) }}
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-7 col-form-label">emailUser</label>
                  <div class="col-sm-10">
              {{ form_widget(f.emailUser,{'attr': {'class':'form-control'}}) }}
                  </div>
                </div>


                 {{ form_start(f) }}
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-7 col-form-label">password</label>
                  <div class="col-sm-10">
              {{ form_widget(f.password,{'attr': {'class':'form-control'}}) }}
                  </div>
                </div>


                 {{ form_start(f) }}
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-7 col-form-label">NumTelephoneUser</label>
                  <div class="col-sm-10">
              {{ form_widget(f.NumTelephoneUser,{'attr': {'class':'form-control'}}) }}
                  </div>
                </div>

               

                  {{ form_start(f) }}
                <div class="row mb-3">
                  <label for="inputText" class="col-sm-7 col-form-label">cinUser</label>
                  <div class="col-sm-10">
              {{ form_widget(f.cinUser,{'attr': {'class':'form-control'}}) }}
                  </div>
                </div>
              
                

                  <div class="col-sm-10">
              {{ form_widget(f.modifier,{'attr': {'class':'btn btn-primaryl'}}) }}
                  </div>
                </div> 

                {{form_end(f) }}

              </form><!-- End General Form Elements -->

            </div>
          </div>

        </div>

{% endblock %}
